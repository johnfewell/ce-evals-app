<!DOCTYPE html>
<html>
  <head>
    <title>Continuing Education Certificates App</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <% if current_user.nil? %>
      <%= render "logged_out_header" %>
    <% elsif current_user.instructor.nil? && current_user.attendee.nil? %>
      <%= render "unassigned_user_nav" %>
    <% elsif !current_user.nil? %>
      <%= render "nav" %>
    <% else %>
      <%= render "logged_out_header" %>
    <% end %>

    <div class="container pt-3">

      <% if !alert.nil? %>
        <div class="flash-messages">
          <div class="flash flash-warn">
            <p id="alert"><%= octicon "alert" %> <%= alert %></p>
          </div>
        </div>
      <% end %>

      <% if !notice.nil? %>
      <div class="flash-messages">
        <div class="flash flash-success">
          <p id="notice"><%= octicon "check" %> <%= notice %></p>
        </div>
      </div>
    <% end %>


      <%= yield %>

      <%= render "footer" %>
    </div>
  </body>
</html>
